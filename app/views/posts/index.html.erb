<% if signed_in? %>
  <%= link_to "Submit a new text post", new_post_path %> 
<% end %>

<div>
  <% @posts.each do |p| %>
    <p><%= link_to p.title, post_path(p) %></p>
    <p>By: <%= p.user.username %></p>
  <% end %>
</div>

